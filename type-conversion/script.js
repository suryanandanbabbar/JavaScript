let amount = 'hello';

// Change String to Number // Take amount = '100'
// amount = +amount;
// amount = parseInt(amount);
// amount = Number(amount);

// Convert Number to String // Take amount = 100
// amount = amount.toString();
// amount = String(amount);

// Convert String to Decimal // Take amount = '99.5'
// amount = parseFloat(amount);

// Convert Number to Boolean // Take amount = 1 or amount = 0
// Only 0 is false.
// Rest all numbers are true.
// amount = Boolean(amount);

// NaN
console.log(1 + NaN);
console.log(Math.sqrt(-1));
console.log(undefined + undefined);
console.log('foo' / 3);

console.log(amount, typeof amount);
